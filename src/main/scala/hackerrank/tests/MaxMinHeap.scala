package hackerrank.tests

class MaxHeap()

//scala.collection.mutable.PriorityQueue
class MaxMinHeap[T: Ordering](values: Array[T], init: T) {
  def apply() = {
    ???
  }
}

object MaxHeap{
  def apply[T: Ordering](values: Array[T], k: Int) = {
    //val heap = Array.ofDim[T](k)
    ???
  }
}